// TODO: Query all posts at once and then selectively return for Previous and Next buttons. Also, /page/1 etc. or ?page=1.
function PostList() {
  return <p>This will be a post list</p>;
}

export default PostList;
